% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZeroAdjustedNegbin.R
\name{ZeroAdjustedNegbin}
\alias{ZeroAdjustedNegbin}
\title{Create a Zero-Adjusted Negative Binomial distribution}
\usage{
ZeroAdjustedNegbin(mu = 1, sigma = 1, nu = 0.3)
}
\arguments{
\item{mu}{The location parameter (mean of the underlying NBI component), written \eqn{\mu} in textbooks.
Must be positive. Defaults to \code{1}.}

\item{sigma}{The dispersion parameter, written \eqn{\sigma} in textbooks.
Must be positive. Defaults to \code{1}.}

\item{nu}{The zero-alteration parameter \eqn{\nu}, the structural probability at zero.
Must be in (0, 1). Defaults to \code{0.3}.}
}
\value{
A \code{ZeroAdjustedNegbin} object.
}
\description{
The Zero-Adjusted Negative Binomial (ZANBI) distribution is a discrete distribution
with support on the non-negative integers. It is a hurdle model combining a point mass
at zero with a zero-truncated negative binomial distribution. This is useful for modeling
count data with excess zeros.
}
\details{
We recommend reading this documentation on
\url{https://zeileis.github.io/distributions3/}, where the math
will render with additional detail and much greater clarity.

In the following, let \eqn{Y} be a Zero-Adjusted Negative Binomial random variable with
\code{mu} = \eqn{\mu}, \code{sigma} = \eqn{\sigma}, and \code{nu} = \eqn{\nu}.

\strong{Support}: \eqn{\{0, 1, 2, \ldots\}}, the non-negative integers

\strong{Probability mass function (p.m.f)}:

\deqn{
    P(Y = 0) = \nu
  }{
    P(Y = 0) = nu
  }

\deqn{
    P(Y = y) = (1 - \nu) \frac{P_{NBI}(y)}{1 - P_{NBI}(0)} \quad \text{for } y \ge 1
  }{
    P(Y = y) = (1 - nu) * P_NBI(y) / (1 - P_NBI(0)) for y >= 1
  }

where \eqn{P_{NBI}} is the negative binomial probability mass function with size = 1/sigma and mean = mu.
}
\examples{

## ZeroAdjustedNegbin() with default parameters
X <- ZeroAdjustedNegbin()
X

## Density and CDF
pdf(X, 0:10)
cdf(X, 0:10)

## Quantiles
quantile(X, c(0.25, 0.5, 0.75))

## Drawing random values
random(X, 5)

## Vectorized parameters
X <- ZeroAdjustedNegbin(mu = c(1, 2), sigma = c(0.5, 1), nu = c(0.2, 0.4))

## Moments
mean(X)
variance(X)

}
\seealso{
Other discrete distributions:
\code{\link[=Bernoulli]{Bernoulli()}},
\code{\link[=Binomial]{Binomial()}},
\code{\link[=Categorical]{Categorical()}},
\code{\link[=Geometric]{Geometric()}},
\code{\link[=HurdleNegativeBinomial]{HurdleNegativeBinomial()}},
\code{\link[=HurdlePoisson]{HurdlePoisson()}},
\code{\link[=HyperGeometric]{HyperGeometric()}},
\code{\link[=Multinomial]{Multinomial()}},
\code{\link[=NegativeBinomial]{NegativeBinomial()}},
\code{\link[=Poisson]{Poisson()}},
\code{\link[=PoissonBinomial]{PoissonBinomial()}},
\code{\link[=ZINegativeBinomial]{ZINegativeBinomial()}},
\code{\link[=ZIPoisson]{ZIPoisson()}},
\code{\link[=ZTNegativeBinomial]{ZTNegativeBinomial()}},
\code{\link[=ZTPoisson]{ZTPoisson()}}
}
\concept{discrete distributions}
